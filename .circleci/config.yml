version: 2
jobs:
  build:
    docker:
      - image: circleci/node:8.15
    steps:
      - checkout
      - run: npm install
      - run: #running the tests
          name: test
          command: npm test